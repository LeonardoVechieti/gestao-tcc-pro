# Conventions — frontend

**Not yet implemented.** Stack is decided (React + PrimeReact), but no code and no
conventions exist yet. Component structure follows atomic design, owned by a
teammate — defer to their conventions once established rather than inventing a
parallel structure here. Fill in: naming, styling approach, state management,
form/validation patterns (should mirror backend VineJS validators where practical),
PT-BR vs EN for UI copy/identifiers (backend domain is entirely PT-BR — see
`.specs/backend-api/.spec/CONVENTIONS.md`).
