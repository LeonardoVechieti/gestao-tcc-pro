# Structure — frontend (`front-tcc-pro/`)

```
front-tcc-pro/
└── design/            # UI mockups (PNG), one per screen — see ARCHITECTURE.md for
                        # the screen-by-screen list
    └── done            # marker file, purpose unclear — do not delete without asking
```

No app source tree exists yet. Will be a React + PrimeReact app scaffolded around a
teammate's atomic-design component structure (`atoms/molecules/organisms/...` or
equivalent) — fill in the real tree once scaffolded.
