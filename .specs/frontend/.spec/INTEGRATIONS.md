# Integrations — frontend

**Not yet implemented.** The only known integration point is `api-tcc-pro`
(backend-api) — see `.specs/backend-api/.spec/STACK.md` for its Swagger docs
(`/swagger` route) and `.specs/backend-api/.spec/CONCERNS.md` for the current
static-token-only auth, which the frontend login screen will need a real login
endpoint to work against.
