# Current Objectives

Updated 2026-07-18 (was stale since `spec-driven init` on 2026-07-01, back when the
frontend didn't exist yet and auth was a static shared token — both have since shipped).

No single feature is formally "in flight" under `.specs/features/<feature>/spec.md`
right now. The de-facto active work is the aluno↔professor orientação flow, tracked in
`.specs/features/fluxo-aluno-professor-orientador/PLANO_IMPLEMENTACAO.md`, whose own
recommended next step is **ORIENT-008 — modelar entregas/documentos** (document
upload/tracking; `/documentos` is currently a frontend placeholder). If security is the
priority instead, that plan recommends **ORIENT-012 — autorização real no backend**.

Update this file when the next feature is explicitly picked up.